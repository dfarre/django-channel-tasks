[asgi]
expose-doctask-api = true

[security]
debug = true
allowed-hosts =
    localhost
proxy-route =

[database]
engine = django.db.backends.postgresql_psycopg2
name = postgres
user = postgres
password = postgres
host = db
port = 5433
install-apps =
    django.contrib.postgres
