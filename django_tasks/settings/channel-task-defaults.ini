[asgi]
expose-doctask-api = true

[security]
allowed-hosts =
    localhost
proxy-route =

[database]
ENGINE = django.db.backends.postgresql_psycopg2
NAME = postgres
HOST = db
USER = postgres
PASSWORD = postgres
PORT = 5433
install-apps =
    django.contrib.postgres
