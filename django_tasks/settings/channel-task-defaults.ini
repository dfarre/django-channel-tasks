[asgi]
expose-doctask-api = true

[security]
allowed-hosts =
    localhost
proxy-route =

[database]
ENGINE = django.db.backends.postgresql_psycopg2
NAME = postgres
USER = postgres
PASSWORD = postgres
HOST = db
PORT = 5433
install-apps =
    django.contrib.postgres
