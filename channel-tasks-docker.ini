[asgi]
expose-doctask-api = true

[apps]
install-apps =
    django.contrib.postgres

[logging]
log-level = DEBUG

[security]
debug = true

[database]
engine = django.db.backends.postgresql_psycopg2
name = postgres
user = postgres
host = db
port = 5433

[redis]
host = redis
