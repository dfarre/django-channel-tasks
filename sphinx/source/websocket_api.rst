Websocket API
^^^^^^^^^^^^^
The web-socket API has (currently) a single endpoint for all background task actions, including the
storage of task results in database JSON fields (documents), and cache-clear requests.
